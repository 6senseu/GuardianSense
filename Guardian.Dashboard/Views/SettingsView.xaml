<UserControl x:Class="Guardian.Dashboard.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" MaxWidth="520" HorizontalAlignment="Left">
            <TextBlock Text="Settings" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>

            <TextBlock Text="Download folder to watch (leave empty for the default Downloads folder)" Margin="0,0,0,4" TextWrapping="Wrap"/>
            <TextBox Text="{Binding DownloadDirectory, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16"/>

            <CheckBox Content="Also watch subdirectories" IsChecked="{Binding MonitorSubdirectories}" Margin="0,0,0,16"/>

            <TextBlock Text="File-ready timeout (seconds)" Margin="0,0,0,4"/>
            <TextBox Text="{Binding FileReadyTimeoutSeconds, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalAlignment="Left" Margin="0,0,0,16"/>

            <Separator Margin="0,0,0,16"/>

            <CheckBox Content="Enable VirusTotal cloud reputation checks" IsChecked="{Binding CloudReputationEnabled}" Margin="0,0,0,8"/>

            <TextBlock Text="VirusTotal API key (get a free key at virustotal.com)" Margin="0,0,0,4" TextWrapping="Wrap"/>
            <TextBox Text="{Binding VirusTotalApiKey, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16"/>

            <Button Content="Save" Width="120" HorizontalAlignment="Left" Command="{Binding SaveCommand}" Margin="0,0,0,8"/>

            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="DarkSlateGray" Margin="0,0,0,16"/>

            <TextBlock Text="Note: Guardian Service must be restarted for changes to take effect."
                       FontStyle="Italic" TextWrapping="Wrap" Opacity="0.7"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
