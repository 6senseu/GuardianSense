<UserControl x:Class="Guardian.Dashboard.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Scan Reports" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <Button Content="Refresh" Width="100" Command="{Binding LoadCommand}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Reports}"
                      SelectedItem="{Binding SelectedReport}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="*"/>
                    <DataGridTextColumn Header="Detected" Binding="{Binding DetectedAtUtc, StringFormat='{}{0:g}'}" Width="140"/>
                    <DataGridTextColumn Header="Risk" Binding="{Binding LocalRiskLevel}" Width="90"/>
                    <DataGridTextColumn Header="Score" Binding="{Binding LocalRiskScore}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>

            <ScrollViewer Grid.Column="1" Margin="8,0,0,0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Details" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"/>

                    <TextBlock Margin="0,0,0,2" TextWrapping="Wrap"><Run Text="Path: "/><Run Text="{Binding SelectedReport.OriginalPath}"/></TextBlock>
                    <TextBlock Margin="0,0,0,2"><Run Text="SHA-256: "/><Run Text="{Binding SelectedReport.Sha256}"/></TextBlock>
                    <TextBlock Margin="0,0,0,2"><Run Text="Signature: "/><Run Text="{Binding SelectedReport.SignatureStatus}"/></TextBlock>
                    <TextBlock Margin="0,0,0,2"><Run Text="Publisher: "/><Run Text="{Binding SelectedReport.Publisher}"/></TextBlock>
                    <TextBlock Margin="0,0,0,8"><Run Text="Cloud reputation: "/><Run Text="{Binding SelectedReport.CloudReputationStatus}"/></TextBlock>

                    <TextBlock Text="Risk reasons:" FontWeight="Bold" Margin="0,0,0,4"/>
                    <ItemsControl ItemsSource="{Binding SelectedReport.LocalRiskReasons}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
